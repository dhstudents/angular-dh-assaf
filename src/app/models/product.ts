// product model
export class Product {
  // public id: number;

  constructor(
    public id?: number ,
    public name?: string,
    public price?: number,
    public stock?: number ) {
      // this.id = id;
  }
}